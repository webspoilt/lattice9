version: '3.8'

services:
  # Node.js Frontend & tRPC Backend
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - DATABASE_URL=postgresql://lattice9:l9_secret@db:5432/lattice9
      - REDIS_URL=redis://redis:6379
      - MINIO_ENDPOINT=minio:9000
      - MINIO_ACCESS_KEY=l9_admin
      - MINIO_SECRET_KEY=l9_secret_storage
      - NEO4J_URI=bolt://graphdb:7687
      - NEO4J_USER=neo4j
      - NEO4J_PASSWORD=l9_graph_secret
      - JWT_SECRET=${JWT_SECRET:-sovereign_secret_key}
      - LATTICE9_ENGINE_KEY=${LATTICE9_ENGINE_KEY:-sovereign-l9-secret-2026}
      - OLLAMA_HOST=http://host.docker.internal:11434
    depends_on:
      - db
      - redis
      - graphdb
      - minio
    networks:
      - l9-net

  # Python Security Engine
  engine:
    build: 
      context: ./server-py
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=postgresql://lattice9:l9_secret@db:5432/lattice9
      - REDIS_URL=redis://redis:6379
      - NEO4J_URI=bolt://graphdb:7687
      - NEO4J_USER=neo4j
      - NEO4J_PASSWORD=l9_graph_secret
      - LATTICE9_ENGINE_KEY=${LATTICE9_ENGINE_KEY:-sovereign-l9-secret-2026}
    depends_on:
      - db
      - redis
      - graphdb
    networks:
      - l9-net

  # PostgreSQL Operational Source of Truth
  db:
    image: postgres:16-alpine
    restart: always
    environment:
      - POSTGRES_DB=lattice9
      - POSTGRES_USER=lattice9
      - POSTGRES_PASSWORD=l9_secret
    ports:
      - "5432:5432"
    volumes:
      - l9_postgres_data:/var/lib/postgresql/data
    networks:
      - l9-net

  # Neo4j Graph Intelligence
  graphdb:
    image: neo4j:5.12-community
    restart: always
    environment:
      - NEO4J_AUTH=neo4j/l9_graph_secret
      - NEO4J_PLUGINS=["apoc"]
    ports:
      - "7474:7474"
      - "7687:7687"
    volumes:
      - l9_neo4j_data:/data
    networks:
      - l9-net

  # Redis Streams & Cache
  redis:
    image: redis:7-alpine
    restart: always
    ports:
      - "6379:6379"
    networks:
      - l9-net

  # MinIO Object Storage (Raw Evidence)
  minio:
    image: minio/minio:latest
    restart: always
    command: server /data --console-address ":9001"
    environment:
      - MINIO_ROOT_USER=l9_admin
      - MINIO_ROOT_PASSWORD=l9_secret_storage
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - l9_minio_data:/data
    networks:
      - l9-net

volumes:
  l9_postgres_data:
  l9_neo4j_data:
  l9_minio_data:

networks:
  l9-net:
    driver: bridge
